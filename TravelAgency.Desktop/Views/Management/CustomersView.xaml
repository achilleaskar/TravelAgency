<UserControl x:Class="TravelAgency.Desktop.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <UserControl.Resources>
        <Style x:Key="RightAligned"
               TargetType="{x:Type TextBlock}">
            <Setter Property="HorizontalAlignment"
                    Value="Right" />
            <Setter Property="TextAlignment"
                    Value="Right" />
            <Setter Property="Padding"
                    Value="0,0,6,0" />
        </Style>
    </UserControl.Resources>

    <DockPanel>
        <StackPanel DockPanel.Dock="Top"
                    Orientation="Horizontal"
                    Margin="8">
            <StackPanel.Resources>
                <Style TargetType="Control">
                    <Setter Property="Margin"
                            Value="0,0,8,0" />
                    <Setter Property="VerticalAlignment"
                            Value="Center" />
                </Style>
            </StackPanel.Resources>

            <TextBox Width="220"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Load"
                    Command="{Binding LoadCommand}" />
            <Separator Width="16" />
            <Button Content="Add New"
                    Command="{Binding BeginNewCommand}" />
            <Button Content="Edit"
                    Command="{Binding BeginEditCommand}"
                    IsEnabled="{Binding CanEdit}" />
            <Button Content="Delete"
                    Command="{Binding DeleteCommand}"
                    IsEnabled="{Binding CanDelete}" />
        </StackPanel>

        <Grid Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding Items}"
                      SelectedItem="{Binding Selected}"
                      IsReadOnly="True"
                      AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name"
                                        Binding="{Binding Name}"
                                        Width="220" />
                    <DataGridTextColumn Header="Email"
                                        Binding="{Binding Email}"
                                        Width="220" />
                    <DataGridTextColumn Header="Phone"
                                        Binding="{Binding Phone}"
                                        Width="160" />
                    <DataGridTextColumn Header="Old Balance"
                                        Binding="{Binding OldBalance, StringFormat={}{0:0.##}}"
                                        Width="120"
                                        ElementStyle="{StaticResource RightAligned}" />
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Column="1"
                          Width="12" />

            <Border Grid.Column="2"
                    Padding="12"
                    BorderBrush="#DDD"
                    BorderThickness="1"
                    CornerRadius="6">
                <StackPanel>
                    <TextBlock Text="{Binding EditorTitle}"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8" />
                    <StackPanel Orientation="Horizontal"
                                Margin="0,0,0,8">
                        <TextBlock Text="Name"
                                   Width="100"
                                   VerticalAlignment="Center" />
                        <TextBox Width="280"
                                 Text="{Binding EditName}"
                                 IsEnabled="{Binding IsEditing}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal"
                                Margin="0,0,0,8">
                        <TextBlock Text="Email"
                                   Width="100"
                                   VerticalAlignment="Center" />
                        <TextBox Width="280"
                                 Text="{Binding EditEmail}"
                                 IsEnabled="{Binding IsEditing}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal"
                                Margin="0,0,0,8">
                        <TextBlock Text="Phone"
                                   Width="100"
                                   VerticalAlignment="Center" />
                        <TextBox Width="200"
                                 Text="{Binding EditPhone}"
                                 IsEnabled="{Binding IsEditing}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal"
                                Margin="0,0,0,16">
                        <TextBlock Text="Old Balance"
                                   Width="100"
                                   VerticalAlignment="Center" />
                        <TextBox Width="120"
                                 Text="{Binding EditOldBalance}"
                                 IsEnabled="{Binding IsEditing}" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <Button Content="Save"
                                Command="{Binding SaveCommand}"
                                IsEnabled="{Binding IsEditing}" />
                        <Button Content="Cancel"
                                Command="{Binding CancelCommand}"
                                Margin="8,0,0,0"
                                IsEnabled="{Binding IsEditing}" />
                    </StackPanel>

                    <TextBlock Text="{Binding EditorHint}"
                               Foreground="Gray"
                               Margin="0,12,0,0" />
                </StackPanel>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
