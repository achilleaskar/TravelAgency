<Window x:Class="TravelAgency.Desktop.Views.ReservationEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Reservation"
        Width="1200"
        Height="760"
        Background="White"
        WindowStartupLocation="CenterOwner">
    <DockPanel>
        <!-- Header -->
        <Grid DockPanel.Dock="Top"
              Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding HeaderTitle}"
                       FontSize="20"
                       FontWeight="Bold"
                       VerticalAlignment="Center" />
            <Button Grid.Column="1"
                    Content="Save"
                    Padding="16,6"
                    Command="{Binding SaveCommand}" />
            <Button Grid.Column="3"
                    Content="Cancel"
                    Padding="16,6"
                    Command="{Binding CancelCommand}" />
        </Grid>
        <TabControl Margin="12">
            <TabItem Header="Main">
                <Grid Margin="8"
                      Grid.IsSharedSizeScope="True">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="420" />
                        <!-- left: fields + availability -->
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="*" />
                        <!-- right: selected lines -->
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="Auto" />
                        <!-- totals -->
                    </Grid.RowDefinitions>
                    <!-- LEFT: Basic info + Availability -->
                    <StackPanel Grid.Column="0"
                                Orientation="Vertical" >
                        <!-- Basic info -->
                        <Border Padding="12"
                                BorderBrush="#DDD"
                                BorderThickness="1"
                                CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160"
                                                      SharedSizeGroup="labels" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="8" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="8" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="8" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="8" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="8" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0"
                                           Text="Customer"
                                           VerticalAlignment="Center" />
                                <ComboBox Grid.Row="0"
                                          Grid.Column="1"
                                          ItemsSource="{Binding Customers}"
                                          DisplayMemberPath="Name"
                                          SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}" />
                                <TextBlock Grid.Row="2"
                                           Text="City"
                                           VerticalAlignment="Center" />
                                <ComboBox  Grid.Row="2"
                                           Grid.Column="1"
                                           ItemsSource="{Binding Cities}"
                                           DisplayMemberPath="Name"
                                           SelectedItem="{Binding SelectedCity}" />
                                <TextBlock Grid.Row="4"
                                           Text="Hotel"
                                           VerticalAlignment="Center" />
                                <ComboBox  Grid.Row="4"
                                           Grid.Column="1"
                                           ItemsSource="{Binding FilteredHotels}"
                                           DisplayMemberPath="Name"
                                           SelectedItem="{Binding SelectedHotel}" />
                                <TextBlock Grid.Row="6"
                                           Text="Check-in"
                                           VerticalAlignment="Center" />
                                <DatePicker Grid.Row="6"
                                            Grid.Column="1"
                                            SelectedDate="{Binding CheckIn}" />
                                <TextBlock Grid.Row="8"
                                           Text="Check-out"
                                           VerticalAlignment="Center" />
                                <DatePicker Grid.Row="8"
                                            Grid.Column="1"
                                            SelectedDate="{Binding CheckOut}" />
                                <TextBlock Grid.Row="10"
                                           Text="Nights"
                                           VerticalAlignment="Center" />
                                <TextBlock Grid.Row="10"
                                           Grid.Column="1"
                                           Text="{Binding Nights}"
                                           FontWeight="SemiBold" />
                            </Grid>
                        </Border>
                        <!-- Availability (under basic info) -->
                        <Border Padding="12"
                                BorderBrush="#DDD"
                                BorderThickness="1"
                                CornerRadius="8">
                            <StackPanel>
                                <TextBlock Text="Find availability"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,6" />
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="5" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <!-- Row 0: Check-in / Check-out -->
                                    <TextBlock Grid.Row="0"
                                               Grid.Column="0"
                                               Text="Check-in"
                                               VerticalAlignment="Center"
                                               Margin="0,0,8,0" />
                                    <DatePicker Grid.Row="0"
                                                Grid.Column="1"
                                                SelectedDate="{Binding CheckIn}"
                                                 />
                                    <TextBlock Grid.Row="0"
                                               Grid.Column="3"
                                               Text="Check-out"
                                               VerticalAlignment="Center"
                                               Margin="12,0,8,0" />
                                    <DatePicker Grid.Row="0"
                                                Grid.Column="4"
                                                SelectedDate="{Binding CheckOut}"
                                                  />
                                    <!-- Row 1: Hotel / Search -->
                                    <TextBlock Grid.Row="1"
                                               Grid.Column="0"
                                               Text="Hotel"
                                               VerticalAlignment="Center"
                                               Margin="0,8,8,0" />
                                    <ComboBox Grid.Row="1"
                                              Grid.Column="1"
                                              ItemsSource="{Binding Hotels}"
                                              DisplayMemberPath="Name"
                                              SelectedItem="{Binding SelectedHotel}"
                                              Margin="0,8,0,0" />
                                    <Button Grid.Row="1"
                                            Grid.Column="4"
                                            Content="Search"
                                            Command="{Binding SearchAvailabilityCommand}"
                                            Margin="12,8,0,0" />
                                </Grid>
                                <DataGrid ItemsSource="{Binding AvailableOptions}"
                                          SelectedItem="{Binding SelectedAvailableOption}"
                                          AutoGenerateColumns="False"
                                          CanUserAddRows="False"
                                          HeadersVisibility="Column"
                                          MaxHeight="220" 
                                          Margin="0,4,0,6">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Hotel"
                                                            Width="*"
                                                            Binding="{Binding HotelName}" />
                                        <DataGridTextColumn Header="Room Type"
                                                            Width="*"
                                                            Binding="{Binding RoomTypeName}" />
                                        <DataGridTextColumn Header="Price/Night"
                                                            Width="110"
                                                            Binding="{Binding PricePerNight, StringFormat={}{0:0.##}}" />
                                        <DataGridTextColumn Header="Free (min)"
                                                            Width="90"
                                                            Binding="{Binding FreeMinOverNights}" />
                                        <DataGridTextColumn Header="Capacity"
                                                            Width="90"
                                                            Binding="{Binding TotalCapacity}" />
                                    </DataGrid.Columns>
                                </DataGrid>
                                <Button Content="Add selected"
                                        HorizontalAlignment="Right"
                                        Command="{Binding AddOptionToReservationCommand}"
                                        CommandParameter="{Binding SelectedAvailableOption}" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    <!-- RIGHT: Selected lines -->
                    <DockPanel Grid.Column="2">
                        <ToolBar DockPanel.Dock="Top">
                            <Button Content="Remove line"
                                    Command="{Binding RemoveSelectedLineCommand}" />
                            <Separator />
                            <TextBlock Text="Total:"
                                       Margin="8,0,4,0"
                                       VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Total, StringFormat={}{0:0.##}}"
                                       FontWeight="Bold"
                                       VerticalAlignment="Center" />
                        </ToolBar>
                        <DataGrid Margin="0,8,0,0"
                                  ItemsSource="{Binding Lines}"
                                  SelectedItem="{Binding SelectedLine}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  MaxHeight="260">
                            <!-- 1–5 rows -> compact -->
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Allotment / Room Type"
                                                    Width="2*"
                                                    Binding="{Binding Display}" />
                                <DataGridTextColumn Header="Qty"
                                                    Width="70"
                                                    Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0}}" />
                                <DataGridTextColumn Header="Price/Night"
                                                    Width="110"
                                                    Binding="{Binding PricePerNight, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:0.##}}" />
                                <DataGridTextColumn Header="Notes"
                                                    Width="*"
                                                    Binding="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" />
                                <DataGridTextColumn Header="Line Total"
                                                    Width="110"
                                                    IsReadOnly="True"
                                                    Binding="{Binding LineTotal, StringFormat={}{0:0.##}}" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                    <!-- Totals footer -->
                    <Border Grid.Row="2"
                            Grid.ColumnSpan="3"
                            Padding="12"
                            BorderBrush="#DDD"
                            BorderThickness="1"
                            CornerRadius="8">
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right">
                            <StackPanel Margin="16,0">
                                <TextBlock Text="Total" />
                                <TextBlock Text="{Binding Total, StringFormat={}{0:0.##}}"
                                           FontWeight="Bold" />
                            </StackPanel>
                            <StackPanel Margin="16,0">
                                <TextBlock Text="Paid" />
                                <TextBlock Text="{Binding PaidTotal, StringFormat={}{0:0.##}}"
                                           FontWeight="Bold" />
                            </StackPanel>
                            <StackPanel Margin="16,0">
                                <TextBlock Text="Balance" />
                                <TextBlock Text="{Binding Balance, StringFormat={}{0:0.##}}"
                                           FontWeight="Bold" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
            <!-- Payments -->
            <TabItem Header="Payments">
                <DockPanel Margin="8">
                    <ToolBar DockPanel.Dock="Top">
                        <Button Content="Add Payment"
                                Command="{Binding AddPaymentCommand}" />
                        <Button Content="Remove"
                                Margin="8,0,0,0"
                                Command="{Binding RemoveSelectedPaymentCommand}" />
                    </ToolBar>
                    <DataGrid ItemsSource="{Binding Payments}"
                              SelectedItem="{Binding SelectedPayment}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              Margin="0,8,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date"
                                                Width="140"
                                                Binding="{Binding Date, StringFormat=\{0:yyyy-MM-dd\}, UpdateSourceTrigger=PropertyChanged}" />
                            <DataGridTextColumn Header="Title"
                                                Width="*"
                                                Binding="{Binding Title, UpdateSourceTrigger=PropertyChanged}" />
                            <DataGridTextColumn Header="Kind"
                                                Width="130"
                                                Binding="{Binding Kind, UpdateSourceTrigger=PropertyChanged}" />
                            <DataGridTextColumn Header="Amount"
                                                Width="130"
                                                Binding="{Binding Amount, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:0.##}}" />
                            <DataGridTextColumn Header="Notes"
                                                Width="*"
                                                Binding="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" />
                            <DataGridTextColumn Header="Updated"
                                                Width="170"
                                                Binding="{Binding UpdatedAtLocal, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" />
                            <DataGridCheckBoxColumn Header="Voided"
                                                    Width="90"
                                                    Binding="{Binding IsVoided, UpdateSourceTrigger=PropertyChanged}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </TabItem>
            <!-- History -->
            <TabItem Header="History">
                <DataGrid Margin="8"
                          ItemsSource="{Binding History}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="When"
                                            Width="170"
                                            Binding="{Binding ChangedAtLocal, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" />
                        <DataGridTextColumn Header="By"
                                            Width="120"
                                            Binding="{Binding ChangedBy}" />
                        <DataGridTextColumn Header="Entity"
                                            Width="140"
                                            Binding="{Binding EntityName}" />
                        <DataGridTextColumn Header="Property"
                                            Width="140"
                                            Binding="{Binding PropertyName}" />
                        <DataGridTextColumn Header="Old"
                                            Width="*"
                                            Binding="{Binding OldValue}" />
                        <DataGridTextColumn Header="New"
                                            Width="*"
                                            Binding="{Binding NewValue}" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
