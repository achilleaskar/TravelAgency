<Window x:Class="TravelAgency.Desktop.Views.AllotmentDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Allotment Details"
        Width="760"
        Height="560"
        WindowStartupLocation="CenterOwner">
    <Border Margin="12"
            Padding="12"
            BorderBrush="#DDD"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding Title}"
                       FontSize="20"
                       FontWeight="SemiBold" />

            <StackPanel Grid.Row="1"
                        Orientation="Vertical"
                        Margin="0,8,0,12">
                <TextBlock Text="{Binding HotelLine}" />
                <TextBlock Text="{Binding DateRange}" />
                <TextBlock Text="{Binding OptionInfo}" />
                <TextBlock Text="{Binding Status}" />
                <TextBlock Text="{Binding CreatedUpdated}"
                           Foreground="Gray" />

                <TextBlock Text="Room Types:"
                           FontWeight="SemiBold"
                           Margin="0,8,0,4" />
                <ItemsControl ItemsSource="{Binding RoomTypes}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="Notes:"
                           FontWeight="SemiBold"
                           Margin="0,8,0,4" />
                <ScrollViewer Height="80">
                    <TextBlock Text="{Binding Notes}"
                               TextWrapping="Wrap" />
                </ScrollViewer>
            </StackPanel>

            <GroupBox Grid.Row="2"
                      Header="Update History">
                <ListBox ItemsSource="{Binding History}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,2">
                                <TextBlock Text="{Binding Header}"
                                           FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Diff}"
                                           Foreground="Gray" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>
        </Grid>
    </Border>
</Window>
