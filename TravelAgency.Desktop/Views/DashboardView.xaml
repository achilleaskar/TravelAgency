<UserControl x:Class="TravelAgency.Desktop.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal"
                    Margin="0,0,0,8">
            <StackPanel.Resources>
                <Style TargetType="Control">
                    <Setter Property="Margin"
                            Value="0,0,8,0" />
                </Style>
            </StackPanel.Resources>
            <DatePicker SelectedDate="{Binding FilterStart}" />
            <DatePicker SelectedDate="{Binding FilterEnd}" />
            <Button Content="Refresh"
                    Command="{Binding RefreshCommand}" />
        </StackPanel>
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Alerts}"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ημερομηνία"
                                    Binding="{Binding DueDate, StringFormat=dd/MM/yyyy}"
                                    Width="120" />
                <DataGridTextColumn Header="Μήνυμα"
                                    Binding="{Binding Message}"
                                    Width="*" />
                <DataGridTextColumn Header="Σοβαρότητα"
                                    Binding="{Binding Severity}"
                                    Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>